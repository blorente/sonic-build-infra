[project]
name = "sonic-build-infra-python-deps"
version = "1.0.0"
requires-python = ">= 3.11"
dependencies = [
    # For utility scripts in sonic-buildimage
    "click == 8.4.2",
    "requests == 2.34.2",
]
